.place-show.container
  .main-info.white-block.row
    .col-md-6.left-block
      %h1.title= @place.title
      = image_tag "ratings/#{@place.rating.round}.png", class: 'rating'
      = link_to @place.category.name, '', class: 'category'
      %p.description= @place.description
      = link_to 'Смотреть на карте', map_index_path, class: 'btn btn-success look-at-map'

      .share
        .icon
          .fb-share-button{"data-href" => "https://developers.facebook.com/docs/plugins/", "data-layout" => "button", "data-mobile-iframe" => "true", "data-size" => "small"}
            %a.fb-xfbml-parse-ignore{:href => "https://www.facebook.com/sharer/sharer.php?u=https%3A%2F%2Fdevelopers.facebook.com%2Fdocs%2Fplugins%2F&src=sdkpreparse", :target => "_blank"} Поделиться

        .icon
          :javascript
            document.write(VK.Share.button(false,{type: "round", text: "Поделиться"}));

        .icon
          .g-plus{"data-action" => "share", "data-annotation" => "none"}

        .icon
          %a.twitter-share-button{:href => "https://twitter.com/share"} Tweet

    - if @place.image1.present? || @place.image2.present? || @place.image3.present?
      .col-md-6.right-block
        .images
          .image1
            = image_tag @place.image1.url
          .bottom-images.row
            .image2.col-md-6
              = image_tag @place.image2.url
            .image3.col-md-6
              = image_tag @place.image3.url

  .map-info.white-block.row
    .col-md-12
      %h2.title Карта места

      .address
        .left-block
          %p
            %strong Адрес:
            = @place.address

          %p
            %strong Координаты:
            = @place.coordinates

        .right-block
          %p
            %strong Добавлено:
            = @place.posted_at

          %p
            %strong Просмотры:
            = @place.visits

    %div{:style => "width: 100%;"}
      #map.place-map{:style => "width: 100%; height: 800px;"}

    .col-md-12
      %h2.title Похожие места в этом районе:

      - if @near_places.present?
        .near-places
          - @near_places.each do |place|
            .place.col-md-3
              = image_tag place.image1.url(:medium)
              = image_tag "ratings/#{@place.rating.round}.png", class: 'rating'
              .name
                = link_to place.title, place_path(place.id)

      - else
        %p Нет мест поблизости 

  .reviews.white-block.row
    .col-md-12
      %h2.title Отзывы


:javascript

  var currentLatLng = {lat: parseFloat("#{@place.latitude}"), lng: parseFloat("#{@place.longitude}")};
  var myOptions = {
    zoom: 10,
    center: currentLatLng,
    mapTypeId: google.maps.MapTypeId.ROADMAP,
    mapTypeControl: false
  };

  var map = new google.maps.Map(document.getElementById("map"), myOptions);

  var marker = new google.maps.Marker({
    position: currentLatLng,
    map: map,
    title: 'Hello World!'
  });


:javascript
  (function(d, s, id) {
    var js, fjs = d.getElementsByTagName(s)[0];
    if (d.getElementById(id)) return;
    js = d.createElement(s); js.id = id;
    js.src = "//connect.facebook.net/ru_RU/sdk.js#xfbml=1&version=v2.7&appId=1143419209026360";
    fjs.parentNode.insertBefore(js, fjs);
  }(document, 'script', 'facebook-jssdk'));

:javascript
  !function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');